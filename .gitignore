node_modules
dist

# Un seul gestionnaire : pnpm (pnpm-lock.yaml). Empêche la régénération d'un
# lockfile npm concurrent qui rendrait les installs non reproductibles.
package-lock.json
examples/react/dist
*.tsbuildinfo
.DS_Store
.env
.env.local
examples/react/.env

# Notes de conception en cours (brainstormings, specs pas encore implémentées) : elles
# décrivent ce qui n'existe PAS dans `src/`, et le repo n'a pas à publier une spec que le
# code contredit.
docs/superpowers/
# Maquettes du compagnon visuel de brainstorming (HTML jetable, même raison)
.superpowers/

# Artefacts de debug (captures Playwright, screenshots de validation)
.playwright-mcp/
/*.jpeg
!/examples/react/src/App.tsx

# Cache Python des scripts de génération de doc
__pycache__/

# Instructions locales de l'assistant IA (contexte de travail, pas du code livré).
CLAUDE.md
